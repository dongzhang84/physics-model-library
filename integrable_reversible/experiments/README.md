# experiments — 训练脚本 + configs

薄薄一层胶水：把 `data/ × models/ × eval/` 串起来，复现论文里具体的图/表。不放模型本体（在 `models/`）。

对应 proposal §2 的执行步骤（按顺序）：

1. 搭可逆 coupling 骨架，验证正向 + 精确逆向（可逆误差 ≈ 0）。← 地基，先做
2. 把 F 设计成局部平移等变算子，在 L=32 任务上训练到训练长度内高准确率。
3. 长度外推评测，与 Transformer、硬编可积画到同一张图。
4. 加守恒漂移诊断、可逆性诊断。
5. 双孤立子可视化：看学出来的交互是否「撞完各自还在」。
6. C 明显赢 Transformer → 主结果成立，开始写；否则调算子/加守恒约束/改任务难度迭代。

`configs/` 放各次 run 的配置。

> 占位目录。开工时逐步填。
